import { createAction } from "@reduxjs/toolkit";

export const setLocation = createAction('SET_LOCATION')
export const setDate = createAction('SET_DATE')
export const setQtyDays = createAction('SET_QTY_DAYS')
